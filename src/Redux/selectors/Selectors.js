export const serverError = (state) => state.profilePage.serverError;
export const currentPageSelector = (state) => state.findUsersPage.currentPage;
export const totalItemsCountSelector = (state) => state.findUsersPage.totalUsersCount;
export const pageSizeSelector = (state) => state.findUsersPage.pageSize;